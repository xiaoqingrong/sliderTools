# sliderTools
slider validation
# use
`<script src="sliderTools.js"></script>`
`let slider = new SliderTools({`
`    el:document.querySelector('.slider')`
`})`
`slider.on('complete',() => {`
`    console.log('验证完成!')`
`})`